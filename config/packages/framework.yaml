# config/packages/framework.yaml
framework:
    secret: '%env(APP_SECRET)%'
    http_method_override: false
    handle_all_throwables: true

    # Configuration minimale pour que les commandes CLI fonctionnent
    router:
        utf8: true
